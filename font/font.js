import { Readex_Pro } from "@next/font/google";

export const readexLight = Readex_Pro({
  subsets: ["latin"],
  variable: "--readexLight",
  weight: ["200"],
});

export const readexRegular = Readex_Pro({
  subsets: ["latin"],
  variable: "--readexRegular",
  weight: ["400"],
});
